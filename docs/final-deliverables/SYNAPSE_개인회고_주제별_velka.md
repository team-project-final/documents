# SYNAPSE 개인 회고 — 주제별 (velka)

- **생성일**: 2026-05-26
- **최종 업데이트**: 2026-06-15 (06-12 직접 작업 잔여 반영 — 주제1에 gitops 인증/검색/관측/IaC 5건(#196/#198/#194/#182/#207)+destroy 선정리·gateway prometheus(#7), 주제2에 shared compose 서비스별 DB initdb(#75) 추가)
- **직전 업데이트**: 2026-06-12 (06-09~06-11 W5 실 통합·라이브 검증·안정화 구간 반영 — 실 EKS dev+staging 16/16 Healthy·ES nori 실기동·bring-up 견고화·Avro 정본 분기 P0/DLT·actuator 401 CrashLoop·learning-ai Kafka SSL·frontend deploy 재발산 방지(G7 해소)·D-043 사인오프·24h 소크 + 06-12 #199 dev·staging 토픽 환경 프리픽스 격리)
- **이전 업데이트**: 2026-06-08 (05-30~06-08 운영 점검·감사·표준화 구간 반영)
- **저자(본인) 별칭**: velka, Qahnaarin, Velkaressia, VelkaressiaBlutkrone (모두 동일 인물, `deepestdark@gmail.com`)
- **구성**: 19개 레포의 내 커밋을 4개 주제로 묶어, 주제당 What/Why/Problem/Solution/회고 통합 표 1개. 05-30~06-08 구간은 [[repo-edit-scope-policy]]에 따라 직접 작업을 gitops·shared·gateway + 문서로 한정하고 서비스 변경은 이슈로 위임한 점검·표준화 성격이라 각 주제 표에 "06-02~06-08 추가" 절로 덧붙였다. 06-09~06-11(W5) 구간은 라이브 통합·릴리스 마감 성격이라 위임만으로 닫히지 않는 **release-blocking 결함**은 서비스 레포도 직접 수정했고(역할: 감사·위임자→실 통합 오너), 각 주제 표에 "06-09~06-11 추가(W5)" 절로 덧붙였다
- **내 커밋 총계**: 약 1,129건 (--all 전수 기준, 2026-05-29 재집계 / 주차 4 구간은 재집계 미수행) · **전체 기간**: 2026-05-09 ~ 2026-06-08
- **레포별 상세본**: [`SYNAPSE_개인회고_velka.md`](./SYNAPSE_개인회고_velka.md) · [아키텍처 현황 보고서(06-08)](./docs/synapse-architecture-status-2026-06-08.md)

## 전체 요약

약 2.5주(2026-05-09~05-26) 동안 SYNAPSE(통합 학습-지식 그래프 SaaS) 폴리레포 프로젝트를 **① 인프라·GitOps·부트스트랩 → ② 백엔드 서비스·골격 → ③ 프론트엔드·시각화 → ④ 문서·PM·사이트** 네 축으로 구축했다. 전 영역에 걸쳐 **spec→plan→구현의 문서 선행 워크플로**, conventional-commit 규율, **FINDING-xxx / D-0xx / F-0x 이슈 번호화 추적**, **라이브 검증 후 멱등·방어적 재수정**이 일관된 강점으로 나타난다. 반대로 약점도 주제를 가로질러 반복된다 — 백엔드 4개 서비스 공통의 **Docker multi-stage Revert→재작성**, 프론트의 **모바일/그리드 CSS 연속 fix**, 문서·사이트의 **정합성 드리프트와 gh-pages 서브경로 배포 실패(base path·모듈 포맷)**, 그리고 일부 빈 커밋 메시지 같은 위생 흔들림. 결함을 ID로 닫고 자동화를 멱등화한 것이 가장 큰 성장이었고, 통합 시점에 환경 제약(CSS source order, 빌드 컨텍스트, 프레임워크 버전, 진입 경로)을 사전 검증하는 것이 공통 개선 지점이다.

**05-27~05-29 추가 작업**: 인프라는 W4 prod 거버넌스(synapse-prod AppProject·manual-sync ApplicationSet·prod-deployer RBAC·Velero 백업/롤백 런북)와 멱등 bring-up.sh 실 EKS 라이브 검증(A2 갭 9건 환류·Redis 키 오배선 교정)·W1/W2 PM 문서 100% 정합을 마무리했고, 백엔드는 4개 서비스 application.yml/JWT 비대칭 표준화와 dev-smoke CI·Flyway starter 교정(flyway-core→starter)으로 설정 결함을 부팅 단계에서 조기 포착하도록 고정했다. 문서는 18 기술 스택 정의서를 6세션(S1~S6) context7+실코드 교차검증으로 픽션 P0 16건을 청산하고 "목표 vs 실재" 박스·ADR 5건으로 v2.3 마감했으며, 신입 온보딩 포털 **synapse-onboarding**(Flutter Web, overview/flow/practice 콘텐츠·Mermaid·GitHub Pages)을 신규 합류시켰다.

**05-30~06-08 추가 작업 (운영 점검·감사·표준화 / [[repo-edit-scope-policy]]상 gitops·shared·gateway + 문서 직접 작업, 서비스는 이슈 위임)**: 만드는 일에서 **origin 기준 감사·표준 수립·위임**으로 무게가 옮겨갔다. ① **Kafka 4서비스 감사**(`docs/kafka-service-audit-2026-06-02.md`)로 engagement 소비측 부재(P0)·CI Schema Registry 미표준화·Avro/serializer 버전 드리프트를 번호화하고, ② **Kafka TLS MSK env-only 미배선 갭**(서비스 커스텀 KafkaConfig가 `security.protocol` 미독)과 **knowledge OpenSearch↔ES 이중 불일치**(dead config + ES9 client↔OpenSearch 비호환)를 규명했다. ③ 인프라 측은 **gitops Kafka SSL Phase 2/3**(KAFKA_ENABLED+SSL env·schema-registry 오버레이)와 **OpenSearch→인클러스터 ES 9.2.1 전환(D-003, PR #114)**, ④ **Flyway 타임스탬프 버전 표준 + CI 가드**(`flyway_guard.py`·reusable `flyway-guard.yml`), **deploy/mirror reusable 표준화 마감(G8)** 을 직접 작업했다. ⑤ 이 모두를 origin 기준으로 재판정한 **2026-06-08 아키텍처 현황 보고서**(간극 G1~G8: 해소 3·진행 4·미해결 1)로 팀 공유용 상태를 고정하고, **main 직행 PR 4건을 dev 재타겟**(충돌 건은 engagement#31·learning#59 이슈 위임)했다. 일관된 교훈: 상태 단언은 `git fetch` 후 origin 기준, env 주입≠앱 배선, 표준은 CI 가드로 강제.

**06-09~06-11 추가 작업 (W5 실 통합·라이브 검증·안정화 / 역할: 감사·위임자→실 EKS·staging 통합 오너 + 크로스커팅 계약·배포 통합자)**: 감사·위임으로 식별만 해 둔 간극을 **실 EKS에 올려 닫는** 릴리스 마감으로 무게가 옮겨갔다. ① **gitops 실 통합** — ES(nori 커스텀 ECR 이미지) 실기동·`es-reindex`/`kafka-topics`/`db-init` bring-up phase·견고화(Windows 경로·orphan ALB/NLB·psql `\gexec`·ruleset BEHIND 레이스)·관측 W3 Step8 마감으로 **dev 9/9 + staging 7/7 → 16/16 Healthy + 24h 소크 진입**. ② **크로스커팅 release-blocker 직접 수정** — Avro writer 정본 분기 P0(engagement#32 `UserRegistered`·learning#64 `NotificationSend`, 06-02 F1~F3의 라이브 발현/DLT)·learning-ai Kafka SSL `ssl_context`(#67)·actuator `/health/**` 401 CrashLoop(engagement#44)·JWT subject 결정적 도출(#33). ③ **frontend deploy semver 통일로 main↔dev 재발산 구조적 방지(#54~#56) → G7 해소**. ④ **shared** W5 일일 staging/통합 리포트·핸드오프 + nori ES ECR 이미지 + node20→24 액션 + platform#87 **DLT 가설 A 입증**. 06-08 G1~G8 대비 **G7(frontend 발산) 해소**, G4(버전 드리프트)는 Avro 정본 정렬로 DLT 근본수정, G6(ES)·G1(Kafka TLS)은 실 EKS 기동으로 검증 진전. 일관된 교훈: 계약은 토픽 네이밍이 아니라 **writer 스키마 정본**까지, 헬스 프로브는 **하위경로까지 permit**, 라이브 EKS 레이스는 **실기동으로만** 잡힘, 배포 태그는 **SHA로 핀**, release-blocker는 위임 말고 **직접 통합**.

## 목차

1. [인프라 · GitOps · 부트스트랩 자동화](#1-인프라--gitops--부트스트랩-자동화) — 213
2. [백엔드 서비스 & 공용 골격](#2-백엔드-서비스--공용-골격) — 158
3. [프론트엔드 & 인터랙티브 시각화](#3-프론트엔드--인터랙티브-시각화) — 176
4. [문서 · PM · 가이드/대시보드 사이트](#4-문서--pm--가이드대시보드-사이트) — 294

---

## 1. 인프라 · GitOps · 부트스트랩 자동화

> **레포**: synapse-gitops(320) · syn(8) · synapse-gateway(10)  ·  합계 338개  ·  기간 2026-05-12 ~ 2026-05-28

| 항목 | 내용 |
| --- | --- |
| **What** | - **Step 1: 폴리레포 부트스트랩 자동화 (syn)** — 멱등 phase 스크립트(`common.sh` 가드 + `lib/repos.sh` 8레포 메타), **Phase 1~3**(레포 생성·브랜치 보호 / 12 secrets·워크플로 7종 / scaffold 5종) 실행 + `BACKEND_STRUCTURE.md` 컨벤션 정의 <br> - **Step 2: EKS/ArgoCD GitOps 기반 (synapse-gitops)** — Terraform IaC(dev), **ArgoCD HA + NLB passthrough**, ApplicationSet 5개 앱, dev→staging 승격(ALB Ingress/TLS·auto-sync), **관측 스택(FR-GO-303~307)**, EBS CSI(IRSA) <br> - **Step 3: 멱등 bring-up + 로컬 MSA 경로 (synapse-gitops)** — phase 기반 **`bring-up.sh`(local/cluster/observability/verify/destroy)**, minikube 인클러스터 인프라(postgres/redis/kafka/opensearch), Flutter 런북 + 통합 Docs Portal(`build_docs.mjs`) <br> - **Step 4: 플랫폼 진입점 게이트웨이 (synapse-gateway)** — Spring Cloud Gateway 5.0.1, 4개 서비스 **path-prefix 라우팅 + Redis Rate Limit(IP 60/min) + CORS**, multi-stage Dockerfile, ECR→gitops 태그 패치 CI/CD <br> - **Step 5: 멱등 bring-up 라이브 검증 + 로컬 k8s 경로 정착 (synapse-gitops)** — `bring-up.sh`를 실 EKS에 처음 돌려 발견한 A2 갭 9건을 코드에 환류, 인클러스터 인프라 5종 + 앱 5개 로컬 overlay + `minikube-up.sh`로 **로컬 11개 파드 풀 그린**, 인터랙티브 SVG 구성도 포함 `docs/local-msa-setup.html` 온보딩 가이드 완성 <br> - **Step 6: W3 마감 + W4 prod 거버넌스 레이어 (synapse-gitops)** — ESO/노드(3→4)/staging ACM·TLS terraform화, prod용 **synapse-prod AppProject + manual-sync ApplicationSet + `role:prod-deployer` RBAC + 4개 앱 prod overlay**, **Velero S3+IRSA 일일 백업 Schedule + 실패 PrometheusRule + 롤백·백업 런북(RTO 30분/RPO 1시간)**(PR #59~#75) <br> - **Step 7: W1/W2 PM 문서 100% 정합화 (synapse-gitops, D-041)** — WORKFLOW 미해결 박스(W1 8→0·W2 13→0) 물리 라인 이동+사유 주석, TASK Status 6 Step 전부 `[x] Done` 통일(PR #76) <br> - **Step 8: reusable deploy 표준화 + OIDC (synapse-gateway)** — 인라인 ECR 빌드/푸시·gitops 태그 sed 치환(약 50줄)을 synapse-shared `deploy-service.yml` caller 호출로 축약, AWS 인증을 정적 access-key→`id-token: write` OIDC로 전환 |
| **Why** | 09 Git 규칙 v2.0 §C1 "Day 1 셋업" 체크리스트대로 4서비스 폴리레포를 **재현 가능하게** 부트스트랩하고, 5개 백엔드 앱을 EKS dev/staging에 GitOps로 자동 배포하는 것이 목표. 신규/팀원이 AWS 없이도 로컬에서 MSA를 띄우도록 minikube 경로·온보딩 가이드를 제공하고, 단일 진입점(gateway)으로 라우팅·남용 방지를 일원화. 반복되는 수동 부트스트랩(터널·argocd·eso·매니페스트)을 멱등 스크립트로 자동화해 1회 실행 재현성을 확보. |
| **Problem** | 라이브 EKS 검증에서 환경 결함 다수(`A2`/`D-0xx` 번호화): private endpoint로 `/readyz` NotFound, ExternalSecret `v1beta1` 미지원, Loki `schemaConfig` 누락으로 loki-0 미생성, EBS CSI 부재, `tfvars` 부재 시 무방비 진행, `setup-dart@v2` 태그 부재. syn phase3는 Tier1 Actions 3/6·mirror dirs 0/6 검증 실패(미러 미동작·Flutter scaffold 누락)와 private 레포 branch protection 불가(Free plan). gateway는 Boot 4.0.6 **YAML 라우트 버그** + gradlew 권한 + `ecr-login@v2` deprecation. 05-27~05-29 추가: bring-up.sh 실 EKS 첫 실행 시 로컬/kind에서 안 보이던 갭 무더기 노출(`/readyz` NotFound, cluster-active waiter가 authenticationMode 미포착, ESO ExternalSecret `v1beta1`→`v1`, Prom/AM 이미지 wget 부재, `--verify` 메트릭 조회 실패), **platform-svc Redis 키 오배선**(앱은 `spring.data.redis.*`만 읽는데 local/dev/staging overlay 모두 `REDIS_HOST/PORT` 키를 줘 localhost 폴백→연결 실패, health group에 redis 부재로 1/1 Ready인 채 장기 잠복). |
| **Solution** | 발견사항을 멱등 수정 — readiness를 `get nodes`로 교체, ExternalSecret apiVersion v1 통일, Loki `schemaConfig`+SingleBinary·memcached 비활성, EBS CSI addon+IRSA·gp3 default, `tfvars` fail-fast, argocd phase 멱등화(`--force-conflicts`). syn은 `mirror.yml`에 `git add -A`+`_mirror` exclude, `frontend-flutter.sh --project-name`, phase1 graceful fallback, deploy skip 모드. gateway는 라우트/CORS를 **Java config(`RoutesConfig`/`CorsConfig`)로 전환** 후 429·CORS 실제 검증, `chmod +x gradlew`, `ecr-login@v3`. 05-27~05-29 추가: A2 발견 9건을 bring-up.sh/lib에 직접 패치(터널 readiness `get nodes`·eks-auth authenticationMode 폴링·argocd `--force-conflicts` 멱등화·ExternalSecret `v1`·tfvars fail-fast)+monitoring README에 ESO 정책·노드≥4 명기, Redis 키를 `SPRING_DATA_REDIS_*` relaxed-binding으로 교정 + SSL/PASSWORD 배선(local PR #57, EKS PR #58). |
| **회고** | "라이브 검증 → 발견 번호화 → 멱등 수정" 루프와 처음부터 멱등(가드·재실행 안전)으로 설계한 부트스트랩이 일관되게 작동해, 추측이 아닌 실제 클러스터 동작에 맞춰 견고화한 점이 핵심 강점. 부트스트랩·배포를 phase 단위(dry-run·verify·destroy 포함)로 분해해 1회성 수동 작업을 재현 자동화로 끌어올렸고, 외부 의존(플랜 제한·레지스트리 미설정·프레임워크 버그)을 graceful fallback과 Java config 전환으로 흡수한 방어적 설계가 반복 패턴으로 자리잡았다. 05-27~05-29: "로컬/kind 그린은 EKS 그린을 보장하지 않는다" — 멱등 자동화의 가치는 실 클러스터 1회 실행에서 드러난 9건의 환경 차이로 입증됐고, 같은 Redis 키 오배선이 local→dev→staging 세 곳에 잠복했던 데서 **probe 그린과 actuator aggregate 그린을 분리해 봐야 한다**는 교훈을 얻었다. prod 거버넌스(manual-sync·RBAC·Velero 백업/롤백)와 PM 문서 정합으로 W4 운영 토대를 닫았다. **06-02~06-08 추가**: gitops에서 Kafka SSL Phase 2/3(KAFKA_ENABLED+SSL env·schema-registry staging/prod 오버레이, PR #118/#119)와 **OpenSearch→인클러스터 ES 9.2.1 전환(D-003, PR #114)**, deploy/mirror reusable 표준화 마감(G8: shared `deploy-service.yml`/`mirror-service.yml` main 머지 + 6레포 caller + `apps/gateway`)을 직접 작업했다. 여기서 **env 주입(gitops)은 필요조건일 뿐 앱 배선(서비스 커스텀 KafkaConfig가 `security.protocol`/`synapse.kafka.enabled`를 읽는 코드)이 선행돼야 충분조건**임을 TLS MSK 갭 조사로 확인하고, 검색은 "프로비저닝됨≠앱이 씀"(env명 불일치 dead config + ES9↔OpenSearch product-check 비호환)을 풀어 엔진을 ES로 정합했다. **06-09~06-11 추가(W5)**: env·계약을 검증만 하던 단계에서 **실 EKS dev+staging을 그린으로 올리는** 단계로 진입했다. ES는 인클러스터 StatefulSet으로 **실기동** — Nori 한국어 분석기는 stock 이미지에 없어 **커스텀 ES 이미지를 ECR에 빌드**(shared#54/#55)하고, `fsGroup`·gp3 SC·배포 **순서를 고정**(gitops#180)했으며 인덱스 존재판정을 404 본문 grep→**HTTP 상태코드**로 교정(#185)했다. bring-up에 `es-reindex`/`kafka-topics`(MSK 9종 SSL Job)/`db-init`(RDS 5 DB) **멱등 phase**를 추가하고, 실 EKS 1회성 결함(`psql -c`가 `\gexec` 미처리→stdin 전달 #171, destroy 시 **orphan ALB/NLB가 VPC teardown 차단**→선정리 #178, Windows kubectl KUBECONFIG 경로 자동변환 #181)을 환류했다. 분기 보호 ruleset도 정상화 — required check 컨텍스트를 **잡명 `validate`** 로 정정(#173), strict 완화로 **main 처닝 BEHIND 레이스 제거**(#165), 이미지는 `dev-latest` 부동 태그→**SHA 핀**(#164)으로 결정성 확보. 관측 W3 Step8(ESO 알람·앱별 대시보드·Grafana nip.io)·EKS 매니지드 컨트롤플레인 룰 false-positive 비활성(#194)까지 마감해 **dev 9/9 + staging 7/7 → 16/16 Healthy + 24h 소크**에 진입하고 D-043 사인오프(겸임)로 W3/W4/W5를 닫았다. 교훈: 라이브 EKS 결함은 1회성 **순서·레이스**라 실기동으로만 잡히고("로컬/kind 그린≠EKS 그린"이 W5에서도 관철), 존재 판정은 **본문이 아니라 상태코드**로, 배포 태그는 **SHA로 핀**해야 비결정성·롤백 불능을 막는다. **06-12 추가 ①(라이브 안정화·인증·검색·관측·IaC + gateway 노출)**: #174 E2E 추적 중 드러난 통합 차단·관측 오탐·destroy 갭을 인프라 측에서 직접 닫았다 — **JWT 공개키 단일출처화(gitops#196)**(gateway 사본 오염 + 서비스 3종 미주입→전수 401을 서명 주체 remoteRef 통일 + 키 주입으로), **검색 인덱싱 3중 단절(#198)**(note-search-sync 토픽·knowledge Redis·`SEARCH_AI_BASE_URL` localhost 폴백 동시 복구), **메모리 cAdvisor 이중합산 오탐(#194)**(`container=""` roll-up 합산 194%→`container!=""` 필터 + platform 실측 상향), **bastion cloud-init IAM 레이스 + ECR IaC 분리(#182)**(`user_data` 암묵 의존성 부재→`depends_on`+재시도, ECR standalone 스택), **ServiceMonitor 비-Spring 3종 제외(#207)** + **destroy 선정리 갭 3건**(field-selector BadRequest·컨트롤러 SG·EBS orphan). gateway는 직접 작업 레포라 **Boot 4.0 actuator prometheus 노출(#7/#8)**(exposure + micrometer 레지스트리 둘 다 필요, 미노출 404→TargetDown 해소, [[gateway-prometheus-boot4]])을 앱측에서 처리해 #207(인프라측 비대상 제외)과 짝을 이뤘다. 교훈: 단일 서명자 자산은 사본 없이 SoT, 파이프라인은 토픽·멱등store·URL이 모두 충족돼야(localhost 폴백 조용한 유실), 관측 알람은 끄기 전 실측으로 진성 신호 분리, 컨트롤러 동적 자산(SG·EBS)은 state 밖이라 명시 reap, Boot 4.0 노출은 설정+레지스트리 둘 다 — 모두 실 EKS/destroy 1회로만 드러난 갭. <br><br> **06-12 추가 ②(#199 토픽 프리픽스)**: dev·staging이 **공유 MSK를 같은 컨슈머그룹**으로 소비해 토픽 파티션이 환경 교차 분산→이벤트 누수(dev 검색 0건·staging DLQ). **토픽 환경 프리픽스**(옵션 B)로 격리 — gitops#206(오버레이 15개 `KAFKA_TOPIC_PREFIX`/`LEARNING_AI_*` 주입 + bring-up이 `""`·dev.·staging.·prod. 4종 멱등 생성, 레거시 병존=무중단)·shared#72(`EVENT_CONTRACT_STANDARD §2.1` 표준)를 직접, 앱 적용은 서비스 이슈 위임(platform#102 해소·learning/engagement dev·knowledge 미착수). 옵션 A(그룹 접미사)는 공유 Redis 멱등store·동일 테넌트 교차오염 위험, C(전용 MSK)는 비용 과다라 기각. 한 클러스터 멀티환경 격리에서도 **env 주입(gitops)≠앱 배선** 원칙이 그대로 — 인프라는 inert로 선행, 충돌 해소는 앱 채택까지. |

---

## 2. 백엔드 서비스 & 공용 골격

> **레포**: synapse-svc-template(93) · synapse-shared(46) · knowledge-svc(17) · engagement-svc(12) · learning-svc(12) · platform-svc(10)  ·  합계 190개  ·  기간 2026-05-12 ~ 2026-05-28

| 항목 | 내용 |
| --- | --- |
| **What** | - **Step 1: 진화형 골격 템플릿 (svc-template)** — **16개 누적 skeleton 브랜치**(W1 도메인 split → W2 `global/` → W3 Kafka 이벤트 → W4 라이트 헥사고날 + ArchUnit/import-linter), **Spring Boot 3.3.5→4.0.6 + Gradle 9.5.1** 업그레이드, 70/70 PASS 검증 <br> - **Step 2: 공용 스키마·인프라 (shared)** — Kafka Avro 4종 + **`{service}.{domain}.{event}-v1` 토픽 체계**, 13서비스 Docker Compose, MSK 보안 절차서, 허브-스포크 핸드오프·E2E 시나리오 <br> - **Step 3: 4개 도메인 서비스 부트스트랩 (platform/knowledge/engagement/learning-svc)** — 공통 레포 골격(README/CODEOWNERS) + `parse-workflow` CI + PM 문서 동기화 + **EKS용 multi-stage Dockerfile** <br> - **Step 4: 서비스별 특화** — platform은 **Spring Modulith 경계**(Auth/User/Billing/Notification/Admin) + dev `ddl-auto` 보정, learning은 **Java(card)+Python(ai) 이중 모듈** <br> - **Step 5: W3 로컬 우선 E2E + reusable WF 표준화 (shared)** — EKS destroy·팀 Kafka PR 0건 현실을 반영해 로컬 docker-compose E2E 중심 플랜 현행화, cross-repo work-order 발행/추적, 로컬 Kafka E2E harness 베이스라인(5/5 토픽 produce→consume, D-1·D-2 발견·해결), 미러/배포 CI를 `mirror-service.yml`/`deploy-service.yml`(workflow_call·OIDC) reusable로 표준화 <br> - **Step 6: 4개 서비스 application.yml/JWT 표준화 + dev-smoke CI** — dev/prod/test 프로파일 분리·`synapse.jwt.public-key` prefix 통일·port 정렬(platform 8081/knowledge 8082/engagement 8083/learning 8084), local/staging yml 제거, **OAuth2 Resource Server + `NimbusJwtDecoder` 검증자 신설**(engagement/learning은 빈 보안 스텁 제거 후 실제 RS256 검증·통합테스트 401 계약 갱신), `docker-compose.ci.yml` 기반 dev-smoke 부팅 헬스 검증, deploy/mirror reusable caller 전환, learning은 `application.properties`→yml 마이그·ai/card 2잡 분리 |
| **Why** | SCOPE 문서상 각 트랙 담당자가 W1에 "기능 개발 가능한 Spring Boot/Modulith 골격 + Dockerfile"을 먼저 세워야 했음. 도메인 간 직접 호출 금지(Kafka only), `global/`만 횡단 의존, `domain/` 외부 의존 0 같은 헥사고날 원칙을 svc-template+ArchUnit으로 표준화·자동 강제하고, shared로 이벤트 스키마·로컬 환경을 일원화. PM 문서가 옛 wiki에 머물러 팀원 혼란이 있어 documents 최신본(@b80635b) 기준으로 동기화. |
| **Problem** | svc-template은 **F-01~F-06** 초기 12개 결함 + SB4 업그레이드 후 빌드 연쇄 깨짐(ArchUnit 1.3.0 API, Jackson 3 Serializer, Consumer bean 충돌, adapter erasure 충돌). shared는 gradlew 미커밋·`.gitignore` jar 순서로 CI 실패 + Avro 중복 스키마 + gateway stub. **4개 서비스 공통으로 Dockerfile 추가→Revert→재작성** 반복(engagement #5→#7, knowledge #14→#16, learning #15→#17) + gradlew 100644 권한 실패. platform은 Fresh RDS에서 `mfa_credentials` 누락으로 JPA validate 실패(D-024) + 대시보드 push race. 05-27~05-28 추가: shared harness에서 stale ZK broker znode로 Kafka 컨테이너 Exited(1)(D-1)·멀티라인 JSON이 줄 단위로 분리돼 CONSUME WARN(D-2), knowledge/learning에서 `flyway-core`만 선언돼 Flyway(V1~V4 / V8~V16) **침묵 미실행**, knowledge ES 통합테스트가 CI에서 TransportException 상시 실패(클라이언트/서버 메이저 불일치+OpenSearch 2.11/Nori 호환), learning은 PR #29 `@EnableCaching`+`RedisCacheManager` 도입 후 Actuator 헬스가 Redis 포함→dev-smoke 게이트 60회(3분) 타임아웃, platform은 dev `synapse.jwt.private-key` 기본값이 손상된 base64라 public-key와 키쌍 불일치→토큰 라운드트립 실패. |
| **Solution** | svc-template은 **cherry-pick 누적 전파**(force-push 없이) 18 fix + ArchUnit predicate/룰 정밀화·Jackson 3 이전·adapter SRP 분리. shared는 wrapper 커밋+gitignore 순서 수정, 중복 스키마 통합, gateway 실제 빌드 교체. 서비스들은 깨진 Dockerfile을 **Revert 후 multi-stage(non-root·레이어 캐싱)로 재작성** + file mode 100755 교정 + `chmod` 안전망. platform은 dev 한정 `ddl-auto: update` + push 3회 재시도. 05-27~05-28 추가: D-1은 `down -v` 볼륨 정리, D-2는 `compact_json`(jq -c) 헬퍼로 1라인 압축(재검증 5/5 OK·13/13 PASS), Flyway는 `flyway-core`→`spring-boot-starter-flyway` 교체(자동설정 활성화)+dev/prod `ddl-auto` none(Flyway를 스키마 권위), ES 테스트는 메이저 정렬·health 대기 시도했으나 OpenSearch/Nori 호환까지 얽혀 `@Disabled`로 CI 제외(별도 과제 트래킹), learning dev-smoke는 `docker-compose.ci.yml`에 `redis:7-alpine` 추가(platform compose 미러링), platform private-key 기본값을 public-key와 짝 맞는 유효 PKCS8 키로 교정. |
| **회고** | 누적 브랜치+cherry-pick 전파로 16개 브랜치에 동형 수정을 force-push 없이 일관 적용하고, ArchUnit으로 의존 규칙을 테스트화해 프레임워크 변경을 빌드 단계에서 조기 포착한 점이 강점. 결함을 F-0x/D-0xx ID로 추적·완결하는 습관과, 기능보다 배포 토대(CI·Docker·CODEOWNERS)를 선행한 흐름이 일관됨. 다만 4개 서비스에서 동일하게 Dockerfile을 Revert→재작성한 반복은 첫 PR 전 빌드 검증을 강화했다면 줄일 수 있었던 공통 시행착오로 남고, "무리한 수정보다 Revert 후 깨끗이 재작성"은 배울 패턴. 05-27~05-28: 클러스터 destroy·팀 PR 0건이라는 불리한 현실을 숨기지 않고 "로컬 우선 E2E + work-order 책임분산 + 정직한 완료 정의"로 재구성한 판단이 정점이었고, 의존성 이름(flyway-core vs starter)이 자동설정을 좌우해 마이그레이션이 조용히 누락될 수 있다는 점·캐싱 도입이 Actuator 헬스에 브로커 의존성을 자동 추가한다는 점에서, **dev-smoke 부팅 헬스 검증으로 침묵 실패를 CI에서 조기 포착하고 의존성을 늘릴 때 CI 인프라도 함께 갱신해야 한다**는 교훈을 얻었다. JWT 키쌍은 prefix 표준화와 키 유효성·키쌍 일치를 함께 검증해야 한다. **06-02~06-08 추가**(서비스 코드는 [[repo-edit-scope-policy]]상 직접 수정 불가 → shared 표준·CI 가드 + 이슈 위임으로 처리): ① Kafka 4서비스 감사로 engagement 소비측 부재(P0, 이후 06-08 EngagementKafkaConsumer로 해소)·CI Schema Registry 미표준화·Avro/serializer 버전 드리프트를 식별하고 shared#14로 **정본 1.11.3/7.5.0 확정**(platform·learning이 정본 초과 → 역방향 정렬 과제). ② platform V28 중복 충돌을 계기로 **Flyway 타임스탬프 버전 표준 + CI 가드**(`scripts/flyway_guard.py`·reusable `flyway-guard.yml`, shared#22 main 머지)를 수립하고 서비스 롤아웃은 이슈(platform#65·knowledge#48·learning#55·engagement#28)로 위임. **표준은 문서가 아니라 CI 가드로 강제해야 지켜지고, 전역 정수 버전 수동 선택은 병렬 브랜치에서 충돌이 시간문제라 단조 정렬 타임스탬프로 구조적으로 제거**해야 한다는 교훈이 더해졌다. **06-09~06-11 추가(W5)**(릴리스 마감 — 위임만으로 닫히지 않는 **release-blocking 결함**에 한해 서비스 레포 직접 수정): 06-02 감사로 번호화만 해 둔 P0가 라이브에서 **DLT·CrashLoop로 발현**해 직접 통합 수정했다. ① **Avro writer 스키마 정본 정렬** — engagement#32 `UserRegistered`(`registeredAt` 제거+공통메타), learning#64 `NotificationSend`(namespace+공통메타) — 토픽 네이밍은 맞아도 **writer 스키마 분기**(namespace·공통메타·필드)로 reader가 해소 실패→DLT. (이후 learning `ReviewCompleted`는 shared#50/#51로 **DLT 가설 A**를 분기 입증해 owner#84가 정본 정렬). ② **actuator `/actuator/health/**` 401 CrashLoop** — `SecurityConfig`가 정확히 `/actuator/health`만 permit해 K8s 프로브 하위경로(`/liveness`·`/readiness`)가 401→livenessProbe SIGTERM 루프(engagement#44·learning-card#79). ③ **learning-ai Kafka SSL** — aiokafka는 `security_protocol=SSL`만으론 부족, `ssl_context` 생성·전달 필요(#67). ④ **JWT subject 결정적 도출** — `userId` claim 부재 시 HTTP·Kafka가 같은 결정적 Long을 쓰도록 통일(engagement#33·knowledge#59). shared 측은 nori ES ECR 이미지·node20→24 액션 업그레이드를 공용에서 올려 owner 이슈로 전파. 교훈: **계약은 토픽 네이밍이 아니라 writer 스키마 정본까지 일치해야 닫히고**(분기는 mock 레지스트리 CI에 안 보이고 런타임 DLT로만 발현 → 전 서비스 BACKWARD 하니스 필요), **헬스 매처는 프로브가 부르는 하위경로(`/**`)까지 permit**(엔드포인트 존재 시 미인증은 404 아닌 401), env 주입≠배선이 Python `ssl_context`에서도 동일, 식별자 도출은 **모든 진입점이 같은 결정적 함수**여야 한다. <br><br> **06-12 추가**: 공용 레포(shared)가 로컬 환경 정의의 단일 출처로서 **compose 단일 DB Flyway 충돌(#75)** 을 구조적으로 막았다 — 여러 서비스가 단일 `synapse` DB의 `flyway_schema_history`를 공유하면 서비스 간 버전(V1·V2…)이 겹쳐 checksum mismatch로 기동 실패하므로, e2e compose에서만 쓰던 `scripts/initdb`를 기본 `docker-compose.yml`에도 마운트해 서비스별 DB(`synapse_platform/engagement/knowledge/learning/ai`)를 최초 기동 시 자동 생성(기존 볼륨은 `down -v` 안내, 각 svc 기본 URL 전환은 svc 레포 이슈로 분리). 교훈: 서비스별 Flyway history는 **DB(또는 스키마) 단위로 격리**해야 한다 — 06-05 platform V28 전역 정수 충돌의 로컬 compose판([[flyway-migration-standard]]). |

---

## 3. 프론트엔드 & 인터랙티브 시각화

> **레포**: synapse-prototype(92) · synapse-flow-simulator(58) · synapse-frontend(28)  ·  합계 178개  ·  기간 2026-05-09 ~ 2026-05-27

| 항목 | 내용 |
| --- | --- |
| **What** | - **Step 1: 시뮬레이터 + Living Docs (synapse-prototype)** — Vite/React **M1~M7 일괄 구축**(DESIGN.md 토큰 ds primitives, 7 Zustand store, SM-2/wikilink/graph TDD, 19라우트), `/docs`·`/tech` 위키 빌드타임 렌더 + Mermaid 시각화 <br> - **Step 2: 플로우 시각화 (synapse-flow-simulator)** — 무빌드 단일 `index.html`, `scenarios.json`(25시나리오·287스텝), **3-모드 시스템(학습/프레젠테이션/레퍼런스)**, UX 뷰·줌·미니맵·딥링킹, Blueprint 다크 테마 <br> - **Step 3: Flutter 앱 (synapse-frontend)** — **ShellRoute+인증 가드** 전환, 갭 분석 후 **52개 화면 + 공통 컴포넌트 9종 완성(9,744줄)**, AdminShell·Graph CustomPaint, 목업 데이터 + `// TODO: 팀원 구현` 분리 패턴 <br> - **Step 4: 접근성·반응형 정비 (공통)** — flow-simulator FINDING-001~010(44px 터치·WCAG AA·ARIA), 모바일 레이아웃·SideNav 안정화 <br> - **Step 5: reusable mirror caller 전환 (synapse-frontend)** — 50여 줄 인라인 rsync/commit/push 미러 스텝을 `uses: .../synapse-shared/.github/workflows/mirror-service.yml@main` + `secrets: inherit` 3줄로 축약, 미러 로직 중앙 일원화 <br> - *(교차 태그)* 05-27 합류한 신입 온보딩 포털 **synapse-onboarding**도 Flutter Web 프론트엔드이나(content/ 마크다운 + Mermaid 렌더 + 검색), 상세는 1차 주제인 §4 문서·PM·사이트에서 다룸 |
| **Why** | 발표·온보딩·개발 레퍼런스 등 혼재된 사용 목적을 **모드 시스템으로 분리**하고, 팀 위키를 빌드타임 동기화해 Living Documentation으로 노출. Flutter는 비즈니스 로직 없이 **순수 UI를 먼저 완성**해 팀원이 Provider 연동만 하면 되도록 책임을 분리. 데모/포트폴리오 품질(ERD 가독성·375px 대응·SPA 딥링크)을 끌어올리려는 의도가 커밋·설계서에 일관. |
| **Problem** | prototype은 **React #185 무한 루프**(useSyncExternalStore 셀렉터가 매 렌더 새 참조 반환) + docs h2 auto-split UX 실패 + Mermaid path traversal 위험. flow-simulator는 **모바일 3컬럼 그리드 0px 붕괴**·CSS source order로 mode-switcher 미숨김·사이드바 폭 과점 등 4~5회 연속 깨짐 + 미등록 Kafka 토픽 5개. frontend는 **Admin 화면 진입/복귀 경로 누락** + PM 문서 stale로 팀원 혼란 + SideNav overflow. |
| **Solution** | prototype은 useEffect deps를 비우고 `getState()` 명령형 실행·안정 셀렉터, auto-split을 통째 **revert해 단일 페이지 회귀**, Mermaid `strict`+docs allowlist 검증. flow-simulator는 `<700px` 단일 컬럼 그리드 + `!important` 우선순위 + `min(220px,75vw)` 클램프, 미등록 토픽 등록. frontend는 `kIsWeb` 조건부 관리자 메뉴+복귀 링크로 양방향 내비, documents @b80635b 기준 재정렬, LayoutBuilder로 overflow 제거. |
| **회고** | lib 도메인 로직·tech 파서를 TDD로 작게 쪼개고, 설계서로 갭(52화면 상태표)을 정량화한 뒤 "공통→핵심→Admin→보강" 순서로 단일 PR에 일관 완성한 점이 효과적. FINDING/D 번호로 접근성·리팩토링을 원자 커밋 추적한 spec-first 흐름이 일관됨. 한편 모바일 CSS와 Admin 내비게이션처럼 "만든 뒤에야 드러난" 문제로 연속 fix가 필요했던 것은 통합 시점에 CSS source order·진입 경로를 동시 검증해야 한다는 교훈이고, 복잡 UX(docs auto-split)는 blanket보다 author opt-in 점진 도입이 낫다는 패턴을 학습. 05-27: frontend 미러 워크플로를 reusable caller로 전환해 서비스별 중복(rsync exclude·커밋 로직)을 제거하고 변경 시 단일 지점만 수정하도록 일원화했다. **06-09~06-11 추가(W5)**: 06-08 보고서에서 **최우선 리스크(G7)** 로 짚었던 frontend의 main↔dev 발산(8/16 상호 발산·리버트 2회)을 **deploy 표준 통일로 구조적으로 닫았다** — 컨테이너 이미지 파이프라인(Dockerfile+deploy.yml, #54)을 추가하고 main을 **semver 모델로 통일**(#55), dev 배포 인프라를 **main 정본과 정합**해 재발산을 방지(#56). 도메인 화면·서비스 연동(검색·learning-ai·admin·결제 등)은 각 owner가 W5에 대량 진행했고 velka의 직접 작업은 **배포 표준 통일**에 한정. 교훈: "어느 쪽이 정본인지 불명확"한 발산은 개별 머지 정리보다 **배포 파이프라인을 단일 정본(semver on main)으로 통일**해 재발산 자체를 구조적으로 제거하는 게 정답이었다 — G7을 만드는 일이 아니라 표준화로 해소. |

---

## 4. 문서 · PM · 가이드/대시보드 사이트

> **레포**: workflow-dashboard(112) · documents(144) · moking-data-guide(27) · schedule-repo(24) · documents.wiki(47) · synapse-onboarding(28) · workflow-guide(13)  ·  합계 395개  ·  기간 2026-05-09 ~ 2026-05-29

| 항목 | 내용 |
| --- | --- |
| **What** | - **Step 1: PM 문서 체계 (documents + documents.wiki)** — **SCOPE/PRD/TASK/WORKFLOW/HISTORY 5종**(7명×W1~W5), 개발 컨벤션 16챕터 Rule북, 위키 18종 명세 + **ADR-001/002(10→4 서비스 통합)** 채택 및 정합 갱신 사이클 <br> - **Step 2: 진행률 대시보드 (workflow-dashboard)** — Vite/React 카드·주차 테이블·추이 차트, **config+파서 레지스트리로 일반화**, `/project-dashboard` 스킬화, Force Sync(PAT·workflow dispatch) <br> - **Step 3: 가이드/일정 사이트 (workflow-guide, schedule-repo, moking-data-guide)** — gh-pages 가이드 매니저, **Gantt/Kanban/주차뷰 일정 SPA(PAT 인증)**, 목킹 전략 8문서 + 19종 시뮬레이터 <br> - **Step 4: 정적 배포 일원화 (공통)** — 모든 사이트를 빌드 파이프라인(MD→JSON)으로 GitHub Pages에 자동 배포 <br> - **Step 5: 18 기술 스택 정의서 6세션 사실검증 + v2.3 마감 (documents + documents.wiki)** — 마스터 설계(437줄)로 6단계 파이프라인(인벤토리→공식문서(context7)→`synapse-*` 실코드 대조→분류→위키 단일 커밋) 표준화, S1 언어·S2a 백엔드·S2b 프론트·S3 데이터·S4 이벤트·S5 운영·S6 외부/AI **6/6 세션 종료**(PR #6~#13), **55개 기술·누적 182 findings**, "보고서 PR(documents) + 위키 단일 커밋(documents.wiki)" dual-commit, 헤더 v2.0→v2.3 마감, §4.1.9 ShedLock·§5.4.1 Outbox Relay·§8.6 운영 ADR 5건 신설, HANDOFF 문서로 세션 이관 체계 확립 <br> - **Step 6: 신입 온보딩 포털 신규 합류 (synapse-onboarding)** — `synapse-gitops/site` Docs Portal을 포크·삭감(gitops 전용 728줄 제거)해 **Flutter Web 온보딩 포털** 신설, 카테고리 **overview/flow/practice 3종**·**서사 9개 섹션**(서비스 상세 4종·샘플 코드 6페이지·Outbox/복제/순서보장 챕터 05b), `content/*.md` 수집 **per-doc JSON + 검색 인덱스 빌드 파이프라인(`build_docs.mjs`)** + **웹 Mermaid 렌더 위젯**, **GitHub Pages 배포 워크플로 + README**, 세부 스펙은 정식 위키로 링크 규약 준수 |
| **Why** | `SCOPE→PRD→TASK→WORKFLOW→HISTORY` 추적 체계로 5주·7인 폴리레포 개발을 담당자·주차별 분해하고, 위키를 **단일 사실원**으로 두어 컨벤션·명세를 통일. 진행 상황·일정·가이드·목킹 개념을 정적 사이트로 무료 공유(읽기 공개·수정 로그인). 하드코딩을 config로 일반화해 재사용 스킬로 발전시키고, 각 개념을 직접 체험할 시뮬레이터를 붙이려는 의도. |
| **Problem** | 대량 문서 병렬 작성으로 **정합성 드리프트**(ERD·API 경로·enum 불일치, OpenSearch→Elasticsearch, ADR 후 03/10/14/17/18 stale). dashboard는 자동 파싱 CI가 **수기 데이터 덮어쓰기·진행률 역행** + 빌드 반복 실패 + Force Sync race. **gh-pages 서브경로 배포 공통 실패**: 자산 base path 404·`fuse` CJS/ESM·SPA 딥링크 404·dnd StrictMode/transform 충돌. 빈 커밋 메시지(`.`)·디버그 산출물 커밋 등 위생 흔들림. 05-27~05-29 추가: 검증으로 위키-실코드 괴리 **16건(P0)** 발견(§6.3 RAG가 "ES+RRF 하이브리드"로 기술됐으나 실재 pgvector cosine 단일·§6.4 Semantic Cache가 "pgvector 캐시 테이블"이나 실재 Redis numpy in-memory, 모델 ID·옛 레포 좌표 `api-gateway/`·`syn/` 잔존), S5 Phase B3 멀티 subagent 디스패치 중 API 529 Overloaded로 세션 중단. synapse-onboarding은 `MarkdownViewer`가 `onTapLink` 없이 렌더해 **본문 마크다운 링크 전부 무반응(no-op)** — 9개 문서의 "다음 읽을거리" 외부 위키 링크가 죽음(`url_launcher` pubspec 선언만·미연결). |
| **Solution** | 위키 단일 기준 **감사→일괄 동기화→재확인(잔여 7건)** 루프, ADR 후 그룹 단위 정합 사이클 후 v2.0.1로 stale 경고 제거. dashboard는 **`trackAliasMap` + rolling max 단조 증가**로 역행 차단, 불안정한 polling 제거 단순화. 사이트는 base path 일괄 치환·**ESM 단일화**·`404.html` fallback·`createPortal` 모달·StrictMode off로 수렴. 05-27~05-29 추가: 미구현 항목을 **"적용 현황(목표 vs 실재)" 박스**로 통일·P0는 실코드 기준 재작성, 운영 정책 결정은 §8.6 ADR 5건으로 명문화·좌표 일괄 정정, 529는 HANDOFF v1.2로 진척 직렬화 후 동일 브랜치에서 Phase B3부터 무손실 재개. onboarding 링크 버그는 `onTapLink`를 비어있지 않은 href에 `launchUrl(externalApplication)`로 연결 + 주입형 `onLinkTap` 콜백·회귀 테스트(PR #1), 배포 액션 Node 24 마이그레이션(PR #2). |
| **회고** | spec→plan→작성 순서와 conventional-commit 분리로 대규모 문서를 추적 가능하게 관리하고, 단일 사실원(위키/config) + 감사·동기화 루프로 정합성 드리프트를 체계적으로 수습한 것이 핵심 패턴. 하드코딩을 config+파서 레지스트리로 일반화 후 스킬로 추출하고 회귀 테스트를 붙인 흐름, "monotonic max"·alias map 같은 멱등·방어 규칙이 효과적이었다. gh-pages 서브경로 배포의 흔한 실패(base path·모듈 포맷)를 직접 겪고 일괄 수렴한 것은 자산이나, 빈 커밋·디버그 산출물 커밋 같은 위생은 처음부터 챙길 여지가 남는다. 05-27~05-29: 5498줄 단일 권위 문서를 6 카테고리로 쪼개 dual-commit(보고서 PR + 위키 단일 커밋)으로 완주하고 context7+실레포 이중 검증으로 문서를 "목표를 현재처럼 단정하던 픽션"에서 진실의 단일 출처로 되돌려 v2.3으로 마감한 것이 핵심 — **권위 문서는 목표와 실재를 분리하지 않으면 픽션이 된다**는 교훈을 "목표 vs 실재" 박스+ADR로 정착시켰다. 신입 온보딩 포털은 검증된 gitops 포털을 포크·삭감해 콘텐츠에 집중하고, 위젯을 주입형 콜백으로 설계해 플랫폼 채널 의존 없이 회귀 테스트를 붙인 점이 잘됐다. **06-02~06-08 추가**: 산출물 무게가 "구축 문서"에서 **origin 기준 감사·현황 보고서**로 옮겨갔다. `docs/kafka-service-audit-2026-06-02.md`(4서비스 Kafka 간극 번호화), Flyway 충돌 spec/plan(`2026-06-05-flyway-history-conflict-*`), 그리고 **2026-06-08 아키텍처 현황 보고서**(`docs/synapse-architecture-status-2026-06-08.md` — 19레포 origin 점검, 간극 G1~G8을 "근거(origin)+추적(이슈)+베이스라인 대비" 한 표로, 해소 3·진행 4·미해결 1)를 팀 공유용으로 작성했다. 핵심 교훈은 **상태 단언 전 반드시 `git fetch` 후 origin 기준**([[verify-merge-state-via-origin]]) — 로컬 클론 stale로 knowledge "미머지"·서비스 "미배선"을 오판할 뻔했고 중복 이슈(#50)를 냈다 닫은 경험에서, 보고서·이슈 작성 전 origin 확인을 게이트로 박았다. 권위 문서를 픽션에서 진실로 되돌리는 일이 회고(v2.3)에서 끝나지 않고 **간극을 번호화해 origin 근거와 함께 재판정하는 현황 보고서**로 이어진 것이 이 구간의 색깔이다. **06-09~06-11 추가(W5)**: 문서 무게가 "현황 보고서"에서 **라이브 통합 일일 핸드오프/리포트**로 옮겨갔다 — gitops `HANDOFF_W5`(잔여→완료 정합·세션 진입점)와 shared **Day3/Day4 staging closeout**(dev+staging 16/16 Healthy·24h 기준점·P0 회귀 FR-ALL-302 PASS), WORKFLOW/HISTORY/TASK의 **W3/W4/W5 실측 정합**(대시보드 10%→~82%, staging 5/5·실도메인 nip.io 임시완결·D-043 사인오프), DLT 가설 A·owner fix-request(스키마 정렬·node24) 발행을 산출했다. 교훈: 라이브 마감 구간에서는 **세션 단위 핸드오프 문서가 다음 세션의 무손실 진입점**이고(잔여 5건→라이브 완료 정합처럼 OPEN/CLOSED를 origin과 맞춰야 함), 대시보드 진행률은 **추정이 아니라 실측(라이브 Healthy·E2E 결과)으로 정합**해야 신뢰가 선다. |
