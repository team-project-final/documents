# 공통 개발 규칙

> **프로젝트명**: Synapse — 통합 학습-지식 그래프 SaaS  
> **버전**: v1.0  
> **작성일**: 2026-05-11  
> **대상**: 전체 팀원 (Backend + Frontend)  
> **목적**: 팀 전원이 통일해야 하는 공통 규칙을 한 문서에 정리

---

## 1. 개요

본 문서는 Synapse 프로젝트의 **전체 팀원**이 공통으로 준수해야 하는 개발 규칙을 정의한다.

### 관련 문서

| 문서 | 역할 |
|------|------|
| [Spring 개발 컨벤션](./Spring_개발_컨벤션.md) | 백엔드 개발자 전용 컨벤션 |
| [Flutter 개발 컨벤션](./Flutter_개발_컨벤션.md) | 프론트엔드 개발자 전용 컨벤션 |
| [wiki 09_Git_규칙_정의서](https://github.com/Public-Project-Area-Oragans/syn/wiki/09_Git_규칙_정의서) | Git 규칙 상세 |
| [wiki 04_API_명세서](https://github.com/Public-Project-Area-Oragans/syn/wiki/04_API_명세서) | API 전체 엔드포인트 |
| [wiki 12_코드_리뷰_규칙](https://github.com/Public-Project-Area-Oragans/syn/wiki/12_코드_리뷰_규칙) | 코드 리뷰 프로세스 상세 |
| [wiki 10_환경_설정_템플릿](https://github.com/Public-Project-Area-Oragans/syn/wiki/10_환경_설정_템플릿) | 환경 설정 상세 |
| [docs/04_operations_security](./04_operations_security.md) | 운영/보안 정책 |

---

## 2. 기본 규칙

> 이 섹션은 **30초 내 핵심 파악**이 가능하도록 테이블/불릿 중심으로 작성되었다.

---

### 2.1 환경 변수 관리

#### 파일 구조

| 파일 | 용도 | Git |
|------|------|-----|
| `.env.example` | 변수 목록 + 설명 (값은 placeholder) | **커밋** |
| `.env.local` | 로컬 개발 실제 값 | `.gitignore` |
| `.env.dev` / `.env.staging` / `.env.prod` | 환경별 비밀 값 | CI/CD Secrets only |

#### 네이밍 규칙

- 형식: `UPPER_SNAKE_CASE`
- 접두사로 서비스/도메인 구분: `PLATFORM_`, `KNOWLEDGE_`, `LEARNING_`, `ENGAGEMENT_`
- 공통 변수: 접두사 없이 `DB_HOST`, `REDIS_URL` 등

#### Good 예제

```bash
# .env.example — 커밋 대상
# ============================================================
# Database
# ============================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=synapse_dev
DB_USER=synapse
DB_PASSWORD=<your-password-here>

# ============================================================
# JWT
# ============================================================
JWT_SECRET=<generate-with: openssl rand -base64 64>
JWT_ACCESS_EXPIRY=900
JWT_REFRESH_EXPIRY=604800

# ============================================================
# External APIs
# ============================================================
OPENAI_API_KEY=<your-openai-key>
STRIPE_SECRET_KEY=<your-stripe-key>

# ============================================================
# Service-specific
# ============================================================
KNOWLEDGE_ES_INDEX_PREFIX=synapse_knowledge
LEARNING_AI_MODEL=claude-sonnet-4-5-20250514
```

#### Bad 예제

```java
// 하드코딩된 비밀 값 — 절대 금지
public class ApiConfig {
    private static final String JWT_SECRET = "my-super-secret-key-123"; // 하드코딩
    private static final String OPENAI_KEY = "sk-proj-abc123...";      // 유출 위험
}
```

```bash
# .env.local을 커밋 — 절대 금지
git add .env.local  # 실제 비밀 값이 레포에 노출
```

> **→ 상세**: [wiki 10_환경_설정_템플릿](https://github.com/Public-Project-Area-Oragans/syn/wiki/10_환경_설정_템플릿)

---

### 2.2 Git 워크플로우 요약

#### 브랜치 네이밍

```
feature/{서비스접두사}-{이슈번호}-{설명}
```

| 서비스 | 접두사 | 예시 |
|--------|--------|------|
| platform-svc | `PLAT` | `feature/PLAT-042-social-login` |
| engagement-svc | `ENG` | `feature/ENG-015-badge-system` |
| knowledge-svc | `KNOW` | `feature/KNOW-088-graph-search` |
| learning-svc (Card) | `LEARN-CARD` | `feature/LEARN-CARD-023-srs-algorithm` |
| learning-svc (AI) | `LEARN-AI` | `feature/LEARN-AI-007-rag-pipeline` |
| frontend | `FE` | `feature/FE-031-note-editor-toolbar` |

#### 커밋 메시지

```
{타입}({스코프}): {제목 — 50자 이내}

{본문 — "왜" 설명}
```

| 타입 | 의미 |
|------|------|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 리팩토링 (기능 변화 없음) |
| `docs` | 문서 변경 |
| `test` | 테스트 추가/수정 |
| `chore` | 빌드/CI/설정 변경 |
| `perf` | 성능 개선 |

#### PR 규칙

| 항목 | 규칙 |
|------|------|
| 크기 | 400줄 이하 권장 (초과 시 분할) |
| 승인 | 팀장 + 도메인 오너 **2인 승인** |
| 브랜치 수명 | 최대 5일 → 강제 PR |
| 보안 관련 | 이중 승인 필수 (팀장 + 보안 담당) |

#### Good 예제

```bash
# 브랜치명
git checkout -b feature/KNOW-042-graph-search-filter

# 커밋 메시지
git commit -m "feat(graph): 검색 필터 옵션 추가

사용자가 노드 유형(노트/카드/태그)별로 그래프 검색 결과를
필터링할 수 있도록 함. 기존 전체 검색은 결과가 너무 많아
원하는 노드를 찾기 어렵다는 피드백 반영."
```

#### Bad 예제

```bash
# 브랜치명 — 정보 없음
git checkout -b feature/new-feature    # 서비스/이슈 없음
git checkout -b fix-bug                # 무슨 버그?

# 커밋 메시지 — 의미 없음
git commit -m "update code"            # 무엇을 왜?
git commit -m "fix"                    # 타입만, 설명 없음
git commit -m "feat: 기능 추가"         # 스코프 없음, 설명 모호
```

> **→ 상세**: [wiki 09_Git_규칙_정의서](https://github.com/Public-Project-Area-Oragans/syn/wiki/09_Git_규칙_정의서) / [docs/SYNAPSE_Git_Rules_Polyrepo_Supplement](./SYNAPSE_Git_Rules_Polyrepo_Supplement.md)

---

### 2.3 API 규약 요약

#### 표준 응답 포맷

**성공:**
```json
{
  "success": true,
  "data": {
    "id": "note-uuid-001",
    "title": "Spring Modulith 학습 노트",
    "createdAt": "2026-05-11T10:00:00Z"
  },
  "meta": {
    "timestamp": "2026-05-11T10:00:00Z",
    "requestId": "req-uuid-abc"
  }
}
```

**에러:**
```json
{
  "success": false,
  "error": {
    "code": "NOTE_NOT_FOUND",
    "message": "요청한 노트를 찾을 수 없습니다.",
    "details": []
  },
  "meta": {
    "timestamp": "2026-05-11T10:00:00Z",
    "requestId": "req-uuid-abc"
  }
}
```

#### 페이지네이션 (커서 기반)

```
GET /api/v1/notes?cursor={lastId}&limit=20&sort=updated_at:desc
```

```json
{
  "success": true,
  "data": ["..."],
  "pagination": {
    "cursor": "next-cursor-value",
    "hasMore": true,
    "totalCount": 150
  },
  "meta": { "timestamp": "...", "requestId": "..." }
}
```

#### Rate Limit

| 대상 | 제한 | 헤더 |
|------|------|------|
| 비인증 | 60 req/min | `X-RateLimit-Limit: 60` |
| 인증 | 600 req/min | `X-RateLimit-Remaining: 599` |
| LLM 엔드포인트 | 30 req/min | `X-RateLimit-Reset: 1620000000` |

#### 멱등성

- **POST** 요청에 `Idempotency-Key` 헤더 필수 (카드 생성, 결제 등)
- 동일 키로 재요청 시 기존 결과 반환 (중복 생성 방지)

#### Good 예제

```bash
# 올바른 API 호출
curl -X POST https://api.synapse.app/api/v1/cards \
  -H "Authorization: Bearer eyJ..." \
  -H "Idempotency-Key: client-uuid-123" \
  -H "Content-Type: application/json" \
  -d '{"front": "Spring AOP란?", "back": "관점 지향 프로그래밍..."}'
```

#### Bad 예제

```bash
# 멱등성 키 없는 POST — 중복 생성 위험
curl -X POST https://api.synapse.app/api/v1/cards \
  -H "Authorization: Bearer eyJ..." \
  -d '{"front": "...", "back": "..."}'  # Idempotency-Key 누락
```

```json
// 비표준 에러 응답 — 절대 금지
{
  "error": true,
  "msg": "not found"
}
```

> **→ 상세**: [wiki 04_API_명세서](https://github.com/Public-Project-Area-Oragans/syn/wiki/04_API_명세서) / [docs/03_api_specification](./03_api_specification.md)

---

### 2.4 코드 리뷰 요약

#### 체크리스트 6축

| 축 | 핵심 확인 항목 |
|----|---------------|
| **기능** | 요구사항 충족, 엣지 케이스 처리, 로직 정확성 |
| **보안** | SQL Injection, XSS, Rate Limit, 인증/인가 |
| **테넌트 격리** | DB 필터, RLS, 캐시 키에 tenantId, Kafka 이벤트에 tenantId |
| **성능** | N+1 쿼리, 불필요한 루프, 인덱스 활용, 캐시 적절성 |
| **가독성** | 네이밍, 함수 크기, 단일 책임, 주석 적절성 |
| **테스트** | 단위/통합 테스트 포함, 커버리지 기준 충족 |

#### 피드백 태그

| 태그 | 의미 | 예시 |
|------|------|------|
| `[MUST]` | 반드시 수정 (머지 차단) | `[MUST] tenant_id 필터 누락` |
| `[SHOULD]` | 강력 권장 (이번 PR에서) | `[SHOULD] N+1 쿼리 — fetch join 추가` |
| `[COULD]` | 다음에 해도 됨 | `[COULD] shared로 추출하면 재사용 가능` |
| `[QUESTION]` | 이해를 위한 질문 | `[QUESTION] 이 타임아웃 값의 근거?` |
| `[PRAISE]` | 칭찬 | `[PRAISE] 이 추상화 깔끔합니다` |
| `[NIT]` | 사소한 스타일 | `[NIT] typo: recieve → receive` |

#### SLA

| 항목 | 기한 |
|------|------|
| 첫 리뷰 | PR 생성 후 **24시간** 이내 |
| 재검토 (수정 후) | 수정 push 후 **48시간** 이내 |

#### Good 예제 (리뷰 코멘트)

```markdown
[MUST] `NoteRepository.findAll()` 에서 tenant_id 필터가 없습니다.
현재 RLS로 방어되지만, 애플리케이션 레이어에서도 반드시 필터링해야 합니다
(이중 방어 원칙).

제안: `findAllByTenantId(TenantContext.getCurrentTenantId())` 로 변경
```

#### Bad 예제 (리뷰 코멘트)

```markdown
이거 왜 이렇게 했어요?          // 태그 없음, 비구체적
이상한데요                      // 무엇이 이상한지, 대안 없음
LGTM                            // 체크리스트 확인 없이 승인
```

> **→ 상세**: [wiki 12_코드_리뷰_규칙](https://github.com/Public-Project-Area-Oragans/syn/wiki/12_코드_리뷰_규칙)
